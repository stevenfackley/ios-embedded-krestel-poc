# ── .NET build output ────────────────────────────────────────────────
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user

# ── NativeAOT / iOS native artifacts (rebuilt by build/publish-ios.sh) ─
build/artifacts/
build/frameworks/
build/publish-*.log
*.xcframework
*.dylib

# ── Xcode (project is generated by xcodegen; build dirs are transient) ─
ios/EmbeddedKestrel.xcodeproj/
ios/build/
DerivedData/
*.xcuserstate

# ── macOS / editor cruft ──────────────────────────────────────────────
.DS_Store
